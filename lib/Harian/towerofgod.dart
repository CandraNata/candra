import 'package:flutter/material.dart';
class towerofgod extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('TOG'),
      ),
      body: Center(
        child: Text('TOG'),
      ),
    );
  }
}